[
  {
    "type": "function",
    "function": {
      "name": "getItemsInPath",
      "description": "Returns all files and folders inside a given directory path.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "Path to get the items from"
          }
        },
        "required": ["path"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "createFile",
      "description": "creates new empty file or overrides pre-existing one into an empty one",
      "parameters": {
        "type": "object",
        "properties": {
          "file": {
            "type": "string",
            "description": "file to be created"
          }
        },
        "required": ["file"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "writeIntoFile",
      "description": "Writes content into a file unless it is forbidden and will create file if file doesn't exist",
      "parameters": {
        "type": "object",
        "properties": {
          "file": {
            "type": "string",
            "description": "Target file path."
          },
          "content": {
            "type": "string",
            "description": "Content to write into the file."
          }
        },
        "required": ["file", "content"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "readFile",
      "description": "Reads and returns the full contents of a file.",
      "parameters": {
        "type": "object",
        "properties": {
          "file": {
            "type": "string",
            "description": "File path to read."
          }
        },
        "required": ["file"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "readFileLines",
      "description": "Reads specific lines from a file. Use this instead of readFile for large files or when you only need a specific section. Line numbers are 1-indexed (line 1 is the first line).",
      "parameters": {
        "type": "object",
        "properties": {
          "file": {
            "type": "string",
            "description": "File path to read."
          },
          "start_line": {
            "type": "integer",
            "description": "The line number to start reading from (1-indexed, inclusive)."
          },
          "end_line": {
            "type": "integer",
            "description": "The line number to stop reading at (1-indexed, inclusive)."
          }
        },
        "required": ["file", "start_line", "end_line"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "delete",
      "description": "Deletes a file at the specified path.",
      "parameters": {
        "type": "object",
        "properties": {
          "file": {
            "type": "string",
            "description": "File path to delete."
          }
        },
        "required": ["file"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "createDirectory",
      "description": "Creates a new directory at the specified path.",
      "parameters": {
        "type": "object",
        "properties": {
          "directory": {
            "type": "string",
            "description": "Directory path to create."
          }
        },
        "required": ["directory"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "deleteDirectory",
      "description": "Deletes a directory at the specified path.",
      "parameters": {
        "type": "object",
        "properties": {
          "directory": {
            "type": "string",
            "description": "Directory path to delete."
          }
        },
        "required": ["directory"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "moveFile",
      "description": "Moves a file from source path to destination path.",
      "parameters": {
        "type": "object",
        "properties": {
          "source": {
            "type": "string",
            "description": "Source file path."
          },
          "destination": {
            "type": "string",
            "description": "Destination file path."
          }
        },
        "required": ["source", "destination"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "copyFile",
      "description": "Copies a file from source path to destination path.",
      "parameters": {
        "type": "object",
        "properties": {
          "source": {
            "type": "string",
            "description": "Source file path."
          },
          "destination": {
            "type": "string",
            "description": "Destination file path."
          }
        },
        "required": ["source", "destination"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "getCurrentDirectory",
      "description": "Returns the current working directory path.",
      "parameters": {
        "type": "object",
        "properties": {}
      }
    }
  },
  {
    "type":"function",
    "function":{
      "name": "runCommand",
      "description": "Runs a shell command and returns its output. Do not resort to this function unless absolutely necessary. You must get user confirmation before running any command.",
      "parameters": {
        "type": "object",
        "properties": {
          "command": {
            "type": "string",
            "description": "The shell command to execute."
          }
        },
        "required": ["command"]
      }
    }
  },
  {
    "type":"function",
    "function":{
      "name": "fileExists",
      "description": "Checks if a file exists at the specified path.",
      "parameters": {
        "type": "object",
        "properties": {
          "file": {
            "type": "string",
            "description": "File path to check."
          }
        },
        "required": ["file"]
      }
    }
  },
  {"type": "function",
    "function": {
      "name": "getFileSize",
      "description": "Returns the size of a file in bytes.",
      "parameters": {
        "type": "object",
        "properties": {
          "file": {
            "type": "string",
            "description": "File path to get the size of."
          }
        },
        "required": ["file"]
      }
    }},
    {
    "type": "function",
    "function": {
      "name": "renameFile",
      "description": "Renames a file from old name to new name. Must include full path in both file and new name.",
      "parameters": {
        "type": "object",
        "properties": {
          "source": {
            "type": "string",
            "description": "The full path of the source file."
          },
          "new_name": {
            "type": "string",
            "description": "The new full path of the file."
          }
        },
        "required": ["source", "new_name"]
      }
    }},
    {
    "type": "function",
    "function": {
      "name": "rememberFact",
      "description": "Remembers a fact by storing it in memory.",
      "parameters": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string",
            "description": "The key associated with the fact."
          },
          "fact": {
            "type": "string",
            "description": "The fact to be remembered."
          }
        },
        "required": ["key", "fact"]
      }
    }},
    {"type": "function",
    "function": {
      "name": "recallFact",
      "description": "Recalls a fact from memory using its associated key.",
      "parameters": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string",
            "description": "The key associated with the fact to be recalled."
          }
        },
        "required": ["key"]
      }
    }},
  {
    "type": "function",
    "function": {
      "name": "forgetFact",
      "description": "Forgets a fact from memory using its associated key.",
      "parameters": {
        "type": "object",
        "properties": {
          "key": {
            "type": "string",
            "description": "The key associated with the fact to be forgotten."
          }
        },
        "required": ["key"]
      }
    }
  },
  {"type": "function",
    "function": {
      "name": "listMemories",
      "description": "Lists all keys of the facts currently stored in memory.",
      "parameters": {
        "type": "object",
        "properties": {}
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "getDirectoryTree",
      "description": "Returns a tree structure representing the directory and file hierarchy starting from the specified path. Returns to a maximum depth of 5 levels.",
      "parameters": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "The path of the directory to start from."
          },
          "depth": {
            "type": "integer",
            "description": "The maximum depth to traverse (default is 2)."
          }
        },
        "required": ["path"]
      }
    }
  }
]
